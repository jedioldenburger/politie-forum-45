# 📝 Comment Formatting - Quick Reference

## 🎨 **Toolbar Buttons**

```
┌─────────────────────────────────────────────┐
│  B  I  <>  "  •  🔗  😊                     │
│  ↓  ↓  ↓   ↓  ↓  ↓   ↓                      │
│  Bold                                        │
│     Italic                                   │
│        Code                                  │
│           Quote                              │
│              List                            │
│                 Link                         │
│                    Emoji                     │
└─────────────────────────────────────────────┘
```

---

## ⚡ **Syntax Quick Reference**

| Want This? | Type This      | Example            |
| ---------- | -------------- | ------------------ |
| **Bold**   | `**text**`     | `**Important**`    |
| _Italic_   | `*text*`       | `*emphasis*`       |
| `Code`     | `` `text` ``   | `` `const x` ``    |
| Code Block | ` ```code``` ` | ` ```js...``` `    |
| > Quote    | `> text`       | `> Citation`       |
| • List     | `- text`       | `- Item one`       |
| Link       | `https://url`  | `https://site.com` |

---

## 🎯 **Common Patterns**

### Make text **bold**:

```
**This is important**
```

### Make text _italic_:

```
*This is emphasized*
```

### Add `inline code`:

```
Use the `useState` hook
```

### Create code block:

````
```
function hello() {
  return "world";
}
```
````

### Quote someone:

```
> User said: Great idea!
```

### Create a list:

```
- First point
- Second point
- Third point
```

### Share a link:

```
Check https://example.com
```

---

## 💡 **Pro Tips**

1. **Select & Format**: Highlight text → Click Bold/Italic
2. **Multiple Formats**: Combine `**bold**` and `*italic*`
3. **Code Sharing**: Use ``` for multi-line code
4. **Quote & Reply**: Use `>` to quote others
5. **Auto-Links**: Just paste URLs (http/https)

---

## 🎨 **Visual Examples**

### Before Formatting:

```
I think the solution is to use useState instead of var.
See docs at https://react.dev
```

### After Formatting:

```
I think the solution is to use `useState` instead of `var`.
See docs at https://react.dev

**Key points:**
- Use hooks
- Avoid var
- Read *official docs*

> "Hooks are the future" - React Team
```

---

## 📱 **Mobile Users**

Tap the toolbar buttons:

- **B** = Bold
- _I_ = Italic
- `<>` = Code
- **"** = Quote
- **•** = List
- **🔗** = Link
- **😊** = Emoji

Or type syntax manually!

---

## ✅ **Preview Your Comment**

Before posting, check:

- [ ] Links are clickable
- [ ] Code has gray/dark background
- [ ] Quotes have left border
- [ ] Lists show bullets
- [ ] Bold/italic work

---

## 🚀 **Ready to Comment!**

**Basic**: Just type and post
**Advanced**: Use formatting for impact
**Pro**: Combine multiple formats

**Happy commenting!** ✨
